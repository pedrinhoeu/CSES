#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0)->sync_with_stdio(0);
    ll n, a, b;
    cin >> n >> a >> b;
    vector<ll> nums(n);
    for(int i = 0; i < n; i++) cin >> nums[i];

    
    
}